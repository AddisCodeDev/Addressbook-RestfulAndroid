<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.addiscan.addiscode.addressbookrestconsume">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application android:name="com.addiscan.addiscode.addressbookrestconsume.Application"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="AA_DB_NAME"
            android:value="Contacts.db" />
        <meta-data
            android:name="AA_DB_VERSION"
            android:value="1" />

        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".NewContactActivity"/>

        <activity android:name=".DetailContactActivity"/>

        <activity android:name=".EditContactActivity"/>

        <activity android:name=".SettingActivity"/>

    </application>

</manifest>
